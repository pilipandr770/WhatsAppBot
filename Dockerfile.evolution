FROM evoapicloud/evolution-api:v2.3.7

# Render auto-detects the actual bound port — no need to force a specific port.
# Evolution v2.3.7 starts on port 8080 by default; Render will route to it.
EXPOSE 8080
